schema:
- name: go-grpc-echo
  path: .
  commands:
    build:
      status: true
    run:
      status: true
      method: ./go-grpc-echo
  args:
  - server
  watcher:
    paths:
    - /
    extensions:
    - go
    ignored_paths:
    - .git
    - .realize.yaml
